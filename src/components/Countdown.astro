---
import { getDaysUntilStart, TRIP_START } from '../lib/dates';

const days = getDaysUntilStart();

const startLabel = TRIP_START.toLocaleDateString('en-US', {
  month: 'long',
  day: 'numeric',
  year: 'numeric',
  timeZone: 'UTC',
});
---

<section class="countdown glass" aria-label="Countdown to trip">
  <span class="label countdown-label">Departing in</span>
  <p class="countdown-number">
    {days}
    <span class="countdown-unit">{days === 1 ? 'day' : 'days'}</span>
  </p>
  <span class="countdown-date">{startLabel}</span>
</section>

<style>
  .countdown {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    padding: 36px 32px;
    text-align: center;
  }

  .countdown-label {
    display: block;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-tertiary);
  }

  .countdown-number {
    font-size: clamp(72px, 18vw, 120px);
    font-weight: 600;
    letter-spacing: -0.04em;
    line-height: 1;
    margin: 0;
    color: var(--text);
  }

  .countdown-unit {
    font-size: 0.22em;
    font-weight: 400;
    color: var(--text-tertiary);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-left: 10px;
    vertical-align: middle;
  }

  .countdown-date {
    display: block;
    font-size: 12px;
    font-weight: 300;
    letter-spacing: 0.06em;
    color: var(--text-secondary);
  }
</style>
